# 后端专用 LZU AppService AES key。
# 必须是 16 字节 UTF-8 字符串；不要提交真实值。
KAIROS_LZU_AES_KEY=replace_16_bytes

# 后续 EasyTong/一卡通功能才会用到 MD5 key。
# 当前代码尚未读取该变量；真实值同样只能放在 src-tauri/.env.local。
KAIROS_LZU_MD5_KEY=replace_with_authorized_value
